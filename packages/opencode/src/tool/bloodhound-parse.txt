Parse BloodHound/SharpHound JSON collection data (computers, users, groups, domains, GPOs). Auto-populates engagement state with discovered hosts, domain info, kerberoastable/AS-REP roastable users, admin accounts, unconstrained delegation, and trust relationships. Accepts individual JSON file content or file path. Auto-detects data type from JSON structure or accepts explicit type hint.